/*class box {
  constructor(x, y, width, height) {
    var wall_options = {
        isStatic:true
    }
    this.body = Bodies.rectangle(x, y, width, height, wall_options);
    this.width = width;
    this.height = height;
    
    World.add(world, this.body);
  }
  display(){
    var pos =this.body.position;
   
    rectMode(CENTER);
    fill("red");
    rect(pos.position.x,pos.position.y, this.width, this.height);
    
  }
};*/